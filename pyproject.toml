[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "S", "SIM"]
ignore = ["S101"]

[tool.coverage.run]
branch = true
source = ["python"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = true
